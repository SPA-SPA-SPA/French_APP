<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arthurlai.ever">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FindWordActivity"
            android:label="@string/FindWordActivity" />
        <activity
            android:name=".WordListActivity"
            android:label="@string/WordListActivity" />
        <activity
            android:name=".AddWordByHandActivity"
            android:label="@string/AddWordByHandActivity" />
        <activity
            android:name=".TestActivity"
            android:label="@string/TestActivity" />
        <activity
            android:name=".TestQActivity"
            android:label="@string/TestQActivity" />
        <activity android:name=".TestQ2Activity" />
        <activity android:name=".SeeTheWordActivity" />
        <activity android:name=".UpdateActivity"></activity>
    </application>

</manifest>